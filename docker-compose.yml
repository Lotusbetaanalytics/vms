version: '3.8'
services:
  web:
    build:
      context: ./app
      target: production
    volumes:
      - .:/app
    command: npm start
    ports:
      - "8000:8000"
    environment:
      NODE_ENV: production
      MONGO_URI: __URI__  
      